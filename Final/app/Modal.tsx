// app/Modal.tsx

import React from 'react';
import { View, Text } from 'react-native';

const Modal = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Modal Placeholder</Text>
    </View>
  );
};

export default Modal; // Ensure this line exists!